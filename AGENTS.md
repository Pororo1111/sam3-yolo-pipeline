# YOLO 파이프라인 WebUI 작업 기준

## 기본 응답 규칙

- 모든 응답은 항상 한국어로 작성한다.
- README, 커밋 메시지, PR 제목과 본문은 항상 한국어로 작성한다.
- 사용자에게 보이는 문서에는 필요할 때 이모지를 적절히 활용한다.

---

## 가장 중요한 원칙

1. **오버엔지니어링 금지**
   - 복잡한 우회 구현, 과한 상태 공유, 불필요한 클라이언트 스크립트, 숨겨진 동작을 만들지 않는다.
   - 프레임워크와 라이브러리가 제공하는 기본 기능을 먼저 사용한다.
   - 공식 예제에서 권장하는 이벤트 흐름과 컴포넌트 갱신 방식을 우선한다.

2. **기초 동작을 먼저 보장**
   - 미리보기 화면은 작업 중 계속 갱신되어야 한다.
   - 탭, 라디오 버튼, 페이지를 왔다 갔다 해도 현재 선택한 화면의 컨텐츠가 사라지면 안 된다.
   - 입력 전환, 시작, 중지, 상태 메시지처럼 기본적인 UI 동작을 먼저 안정화한다.

3. **단순한 구조로 리팩토링**
   - 기존 코드를 한 번에 뒤엎지 않는다.
   - 새 리팩토링 코드는 `refactor_app/` 폴더에서 페이지 단위로 작성한다.
   - 첫 번째 페이지부터 작게 완성하고, 동작 확인 후 다음 페이지로 넘어간다.

4. **공식 문서와 현재 버전 우선**
   - 현재 고정된 의존성 버전을 기준으로 동작을 확인한다.
   - 프레임워크 사용법이 애매하면 공식 문서와 공식 예제를 먼저 확인한다.
   - 자체 해결책을 만들기 전에 라이브러리 기본 기능으로 해결 가능한지 먼저 본다.

---

## 프로젝트 목표

YouTube URL, 웹캠, 비디오 파일, 이미지 폴더를 입력으로 받아 프레임 추출, SAM3 오토라벨링, 데이터셋 구성, YOLO 학습, 추론, 침입 감지까지 이어지는 WebUI를 제공한다.

```text
[1] 소스 선택 & 프레임 추출
    ↓
[2] SAM3 오토라벨링
    ↓
[3] 데이터셋 검토 & 구성
    ↓
[4] YOLO 학습
    ↓
[5] 추론
    ↓
[6] 침입 감지
```

---

## 현재 주요 의존성

| 역할 | 현재 기준 |
|------|-----------|
| UI | Gradio 6.16.0 |
| 영상 처리 | OpenCV 4.13.0 |
| YouTube 입력 | yt-dlp 2026.3.17 |
| SAM3 / YOLO | Ultralytics 8.4.61 |
| HTTP 호출 | requests 2.34.2 |

---

## 리팩토링 폴더 기준

- 새 구현은 `refactor_app/` 아래에서 진행한다.
- 페이지별 UI는 `refactor_app/pages/` 아래에 둔다.
- 기존 앱을 바로 망가뜨리지 않기 위해 기존 `app.py`는 검증 전까지 보존한다.
- 공통 처리 로직이 필요하면 기존 `pipeline/` 모듈을 우선 재사용하고, 정말 필요한 경우에만 작고 명확한 헬퍼를 만든다.

---

## 페이지 구성 기준

- 페이지 전환과 입력 전환은 프레임워크의 기본 이벤트 갱신 기능을 사용한다.
- 화면에 보여야 하는 컴포넌트는 이벤트 출력으로 명확히 갱신한다.
- 미리보기 이미지는 단일한 출력 영역으로 시작하고, 반드시 작업 중 상태와 함께 갱신되게 한다.
- UI가 복잡해지면 기능을 줄여서라도 기본 동작을 안정화한다.

---

## 페이지별 기능 범위

### 1. 소스 선택 & 프레임 추출

**목표**: YouTube URL, 웹캠, 비디오 파일, 이미지 폴더를 원본 프레임으로 저장하고 진행 미리보기를 안정적으로 갱신한다.

**필수 기능**

- 소스 선택
- 소스별 입력 영역 표시
- 샘플 URL, 샘플 비디오, 샘플 이미지 폴더 불러오기
- 캡처 시작
- 캡처 중지
- 진행 미리보기 갱신
- 저장 상태 메시지 표시

**주의할 점**

- 미리보기는 모든 소스에서 같은 영역에 갱신되게 한다.
- 라디오 버튼을 바꿔도 현재 선택한 입력 영역이 즉시 보여야 한다.
- 중지 후 다시 시작할 수 있어야 한다.

### 2. SAM3 오토라벨링

**목표**: 텍스트 프롬프트로 샘플 미리보기를 확인한 뒤 전체 프레임에 라벨을 저장한다.

**필수 기능**

- 프롬프트 입력
- 미리보기 실행
- 전체 라벨링 실행
- 중지
- 진행 미리보기와 상태 메시지 갱신

### 3. 데이터셋 검토 & 구성

**목표**: 라벨 결과를 확인하고 학습용 train/val 데이터셋을 구성한다.

**필수 기능**

- 클래스 목록 확인과 이름 편집
- 라벨 오버레이 미리보기
- 라벨 없는 프레임 필터
- 선택 이미지 삭제
- train/val 구성

### 4. YOLO 학습

**목표**: 구성된 데이터셋으로 모델을 학습하고 결과를 새 폴더에 저장한다.

**필수 기능**

- 학습 설정 입력
- 베이스 모델 선택
- 학습 시작과 중지
- 로그 표시
- 결과 모델 경로 안내

### 5. 추론

**목표**: 학습된 모델로 영상 또는 이미지 폴더 추론을 실행한다.

**필수 기능**

- 모델 선택
- 소스 선택
- 추론 시작과 중지
- 추론 미리보기와 상태 메시지 갱신

### 6. 침입 감지

**목표**: 자연어로 감시 영역을 설정하고 영역 안 객체를 실시간으로 표시한다.

**필수 기능**

- 모델 선택
- 소스 선택
- 감시 영역 설명 입력
- 영역 설정
- 감시 시작과 중지
- 영역 오버레이와 상태 메시지 갱신


---

## 구현 전 체크리스트

- [ ] 공식 예제 방식으로 해결 가능한지 먼저 확인했다.
- [ ] 불필요한 상태 저장이나 복잡한 우회 구현을 추가하지 않았다.
- [ ] 라디오 버튼 전환 후 입력 영역이 정상 표시된다.
- [ ] 작업 중 미리보기가 실제로 갱신된다.
- [ ] 중지 후 다시 시작할 수 있다.
- [ ] 오류 상황은 예외 방치 대신 사용자 메시지로 안내한다.
- [ ] 변경 범위가 페이지 단위로 작게 유지된다.
